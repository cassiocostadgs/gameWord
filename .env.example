# Supabase Cloud configuration (frontend)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Supabase Cloud configuration (backend)
# Use a Service Role Key, não a anon key, para que o servidor possa inserir registros.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key
SERVER_PORT=4000

# Crie a tabela `game_history` com campos:
# durationSeconds (integer), hintsUsed (integer), totalCells (integer), correctCells (integer), completedAt (timestamp)
